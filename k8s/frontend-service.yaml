apiVersion: v1
kind: Service
metadata:
  name: jpeg-to-pdf-frontend
  labels:
    app: jpeg-to-pdf-frontend
    tier: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: jpeg-to-pdf-frontend

