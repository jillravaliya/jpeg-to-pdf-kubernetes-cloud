apiVersion: v1
kind: Service
metadata:
  name: jpeg-to-pdf-backend
  labels:
    app: jpeg-to-pdf-backend
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: jpeg-to-pdf-backend

